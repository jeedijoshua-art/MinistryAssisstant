export const ChatAPI = {
  sendMessage: async (msg: any) => { return {}; }
};

export const CreativeStudioAPI = {
  generate: async (prompt: string) => { return {}; }
};

export const BibleAPI = {
  getVerse: async (verse: string) => { return {}; }
};
